ToDoize want to be an opensource, free and secure ToDo list.


== Build ==

autoreconf -vfi && ./configure && make && make install


== CI ==
A jenkins server is available here : http://jenkins.cthuun.fr
